import React from 'react';
import './Toast.scss';

const Toast = () => {
  // Đây là Toast mẫu, bạn có thể tích hợp context hoặc props để hiển thị thông báo thực tế
  return (
    <div className="toast-global">
      {/* Thông báo toàn cục sẽ hiển thị ở đây */}
    </div>
  );
};

export default Toast; 